package project.controllers;

import javafx.fxml.Initializable;
import project.viewer.ViewManager;

import java.net.URL;
import java.util.ResourceBundle;

public class AdminDashboardController extends BaseController implements Initializable {
    public AdminDashboardController(ViewManager viewManager, String fxmlName) {
        super(viewManager, fxmlName);
    }

    @Override
    public void initialize(URL url, ResourceBundle resourceBundle) {

    }
}
